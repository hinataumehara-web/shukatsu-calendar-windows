# はじめかた（Windows・15分）

締切を自動で集めて、あなたの Google カレンダーに入れるツールです。

就活サイトの締切を自動で拾って、カレンダーに入れるツールです。
**Google アカウントの設定やAPIの登録は要りません。**

困ったら [README.md](README.md) の「うまくいかないとき」を見てください。

---

## 1. Python を入れる（3分）

[python.org のダウンロードページ](https://www.python.org/downloads/windows/)から
Python 3.12 のインストーラを取り、実行します。

> **最初の画面で「Add python.exe to PATH」に必ずチェックを入れてください。**
> ここを飛ばすと後の手順が全部失敗します。

## 2. ファイルを置く（1分）

このリポジトリの緑の **Code** ボタン → **Download ZIP** で落として展開し、
`C:\shukatsu-calendar` に置きます。

> 置き場所は**浅くて、日本語やスペースを含まないパス**にしてください。
> OneDrive の「ドキュメント」フォルダの中は、同期で壊れることがあるので避けます。

## 3. `setup.bat` をダブルクリック（3分）

必要なものが自動で入ります。`Setup finished.` と出たら成功です。
黒い画面が出て文字が流れますが、放っておいて大丈夫です。

## 4. 設定画面を開く（3分）

`setup_gui.bat` をダブルクリックすると、設定用のウィンドウが開きます。
**ここから先はファイルを編集する必要はありません。**

### 「1. カレンダー」タブ

**卒業予定年**が正しいか確認します（3年生・院1年なら「今の年度 + 2」。
2026年度なら 2028）。マイナビはこの年でアクセス先が変わるので、
ここが合っていないと別の学年のページを見にいってしまいます。

つぎに **「Google でログイン」** を押します。ブラウザが開くので、
締切を入れたい Google アカウントを選び、カレンダーへのアクセスを許可してください。
これで以後は自動でカレンダーに登録され、**スマホにも通知が届きます**。

> **「このアプリは確認されていません」と出ます。** 個人が作ったツールで
> Google の審査を通していないためで、故障ではありません。
> 「詳細」→「（アプリ名）に移動」で進めます。渡してくれた人が作ったツールです。
> 求められる権限は**予定の読み書きだけ**です。

Google を使いたくない場合は、ここを飛ばして手順5（ICS ファイル）に進んでください。

### 「2. 就活サイト」タブ

使いたいサイトにチェックを入れます。

- **マイナビ・リクナビ** — 「ブラウザでログインする」ボタンを押します。
  ブラウザが開くので手でログインし、自分のマイページが見える状態にしたら、
  一緒に開いた黒い画面に戻って **Enter**。以後は自動で開きます
  （2段階認証があるので、パスワードを保存する方式では通れません）
- **type就活・ビズリーチキャンパス・外資就活** — メールアドレスとパスワードを
  欄に入力します

> 入力した内容はこのフォルダの中だけに保存されます。ネットには送られません。

### 「3. 実行と自動化」タブ

**「テスト実行（書き込まない）」** を押します。拾えた締切が下のログに出れば成功です。
カレンダーにはまだ何も書き込まれません。

問題なければ **「カレンダーに登録」**。これで完了です（手順5は不要）。

Google を使わない場合は代わりに **「ICS ファイルを書き出す」** を押し、
手順5に進んでください。

最後に **「保存」**。

## 5.（Google を使わない場合だけ）ICS で取り込む

`ics.bat` をダブルクリックすると `shukatsu.ics` が作られ、
「Google カレンダーのインポート画面を開きますか？」と聞かれるので **Enter**。

開いた画面で「インポート」を選び、`C:\shukatsu-calendar\shukatsu.ics` を指定します。
3日前と前日に通知が来る予定として入ります。

以降は、思い出したときに `ics.bat` → インポート、を繰り返すだけです。
**同じ締切が二重に入ることはありません**（予定ごとに固定の ID を付けてあるため）。

---

## よくあるつまずき

| こうなったら | こうする |
|---|---|
| 「python が見つかりません」 | 手順1の PATH のチェックを入れ忘れています。Python を入れ直してください |
| 設定画面が開かない | `setup_gui_error.log` ができていないか見てください。「tkinter が無い」と出る場合は、Python を入れ直すとき「tcl/tk and IDLE」のチェックを外さないでください |
| 黒い画面が一瞬で消える | `.bat` を右クリック →「編集」ではなく、そのままダブルクリックしてください |
| 締切が0件 | サイト側の作りが変わった可能性があります。渡してくれた人に連絡してください |
| 「ログイン状態が確認できません」 | 就活サイトのセッションが切れています。設定画面の「ブラウザでログインする」をやり直してください |
| Google の警告画面から進めない | 「詳細」→「（アプリ名）に移動」を押します。渡してくれた人が作ったツールなので問題ありません |
| 「Google に接続していません」 | 設定画面の「1. カレンダー」タブで「Google でログイン」を押してください |
| 日本語が `?` に化ける | 画面の表示だけの問題です。`shukatsu.log` には正しく残っています |

## 注意

- 各サイトの利用規約を確認のうえ、**自己責任**で使ってください
- 短時間に何度も実行せず、**1日1回程度**にしてください
- 拾い漏れ・誤検出は起こりえます。**大事な締切は必ず公式サイトで確認**してください
- ログイン情報は自分のものを使ってください（ID の共有は各サイトの規約違反です）
